{{- define "mesopotamia.jobs.job" }}
{{- range $context := .Values.services.jobs }}

{{- $files := include "gondwana.files.getList" (dict "files" $context.files "filesFrom" $context.filesFrom "name" $context.name "root" $) | fromYamlArray }}

{{- if $context.cron }}
{{- include "gondwana.skeleton.cronJob" (dict "context" $context "ports" $context.ports "volumes" $context.pvc "files" $files "env" $context.env "root" $) }}
{{- else }}
{{- include "gondwana.skeleton.job" (dict "context" $context "ports" $context.ports "volumes" $context.pvc "files" $files "env" $context.env "root" $) }}
{{- end }}
{{- end }}
---
{{- end }}
