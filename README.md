# Mesopotamia

Mesopotamia is a universal, modular Helm library chart designed to deploy any application with plug-and-play ease.

Built specifically for developers who aren't Kubernetes or Helm Experts, Mesopotamia bridges application-level and infrastructure-level configurations into a single, highly readable `values.yaml` file. By abstracting away the complex boilerplate of Kubernetes manifests, it allows teams to focus on their apps while ensuring a robust, standardized deployment structure underneath. Using a modular approach, you can define different types of micro-services in your application, please see the values examples for more details.

## Getting Started

These instructions will guide you through including the Mesopotamia library chart in your project to handle your application deployments.

### Prerequisites

To use Mesopotamia, you will need the following installed and configured on your cluster:

- Helm v3+
- Kubernetes v1.25+ (or OpenShift 4.12+)

### Installing

Because Mesopotamia is built as a **Library Chart**, it is not deployed on its own. Instead, you include it as a dependency in your application's Helm chart.

1. Open your application's `Chart.yaml` file.
2. Add Mesopotamia to your `dependencies` block:

```yaml
apiVersion: v2
name: <app-name>
description: A Helm chart for Kubernetes
type: application
version: 0.1.0
appVersion: "1.0.0"

dependencies:
  - name: mesopotamia
    version: 0.1.5
    repository: "oci://ghcr.io/yonatan-shabot"
```

3. Add the following file anywhere in the directory and name it 'Mesopotamia.yaml'

```yaml
{ { - include "Mesopotamia" $ } }
```

4. Update your chart dependencies to download Mesopotamia:

```Bash
helm dependency update
```

5. Create a values.yaml file with your application's values, please follow the ".values-example.yaml" for more documentation about the values schema, the most basic one can look like this:

```yaml
release:
  <service-name>: <image-tag> # service name == image name unless the image variable in the service's section
commons:
  imageRepo: <registry>/path/to/repo
services:
  generic:
    - name: <service-name>
```

This minimal values file will create a deployment for your microservice, and by adding just 2 more lines line you can add any microservice you like 6. You can now define your clean, unified configurations in your values.yaml and let Mesopotamia render the underlying Kubernetes resources.

### Running the tests

Automated testing suites for the Mesopotamia chart are currently in development and will be available in future releases.

### Deployment

When deploying your applications to a live production cluster using Mesopotamia, pay special attention to your Ingress configurations.

Ensure that:
A valid Ingress Controller (e.g., NGINX, Traefik, HAProxy) is deployed and actively running on your target production cluster to handle the ingress resources generated by Mesopotamia.

This is deployed best using ArgoCD, but you can also use helm install/upgrade or any other deployment method as long as it supports helm.

### Built With

Helm - The package manager for Kubernetes.
Gondwana - The library chart providing the skeletons to create the templates from (learn more [here](https://github.com/yonatan-shabot/gondwana.git)).

### Contributing

Please read CONTRIBUTING.md for details on our code of conduct, and the process for submitting pull requests to us.

### Versioning

We use SemVer for versioning. For the versions available, see the tags on this repository.

### Authors

Yonatan Shabot - Initial work - @Yonatan-Shabot

For business inquiries / suggestions (keep in mind: this is my first open-source project, complaints / suggestions are more than welcome), please reach out at: shabotyonatan@gmail.com

### License

This project is licensed under the MIT License - see the LICENSE file for details.
